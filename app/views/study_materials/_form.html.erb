<%= form_with(model: @study_material, local: true) do |f| %>

  <%= f.label :picture %>
  <%= f.file_field :picture %>

  <%= f.label :name %>
  <%= f.text_field :name %>
  
  <%= f.submit "Post" %>
<% end %>