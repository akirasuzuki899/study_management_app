<!DOCTYPE html>
<html>
  <head>
    <title>StudyManagementApp</title>
    <%= render 'layouts/rails_default' %>
  </head>

  <body>
    <%= render 'layouts/header' %>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
    <%= debug(params) if Rails.env.development? %>
  </body>
</html>
